# QDII 申购限额监控台 · 项目交接文档

> 本文档供接手开发使用。记录已完成的工作、**验证过的事实**、已知缺陷与下一步方向。
> 所有结论都经过实测验证，带证据；未验证的推测已明确标注。

## 〇、2026-09-25 架构更新：已移除安鑫乐运行时依赖

当前生产采集链不再请求 `anxinletech.com`。下文关于安鑫乐的内容保留为历史溯源记录。

- 基金清单、主题和迁移基线：`data/fund-catalog.json`
- 新份额发现：天天基金 `fundcode_search.js`，仅接纳可明确归入既有12个主题的人民币份额
- 代销实时状态：天天基金单只基金页
- 直销增量：`api.fund.eastmoney.com/f10/JJGG` 销售公告列表
- 公告正文：`np-cnotice-stock.eastmoney.com/api/content/ann`
- 公告解析与生效日状态机：`src/official-direct.js`
- 已解析公告缓存：`data/direct-state.json`

只有高置信解析且已到生效日的公告才覆盖目录基线；未知措辞沿用最后一次可靠值并写入采集错误。迁移后实测162只基金的核心字段和统计与迁移前完全一致，公告列表120只扫描成功、天天基金162只采集成功，均为0失败。

---

## 一、项目缘起

用户提供了一个网站 `https://pmtools.com.cn/qdii`，要求搞清楚它是做什么的、数据从哪来，然后**自建一个 UI 版监控系统，每天监控 QDII 基金申购额度并推送通知**。

已完成全部三项，产出位于 `D:\deepseek\workspace\nasidake\qdii-monitor\`。

---

## 二、背景调查结论：pmtools.com.cn 是什么

一个个人开发者的作品集站（React SPA，nginx/Ubuntu 托管），GitHub 账号 `xujianjun95`。三个板块：`看项目`（作品集）、`看资讯`（AI 资讯流）、`QDII 监控`。

真正的价值功能是 `/qdii`：追踪场外 QDII 基金的申购状态与单日购买上限。

### pmtools 的数据来源（源码级证据）

站点是纯前端 SPA，内容全在 JS bundle 里。从 `/assets/index-DKUSia-s.js` 挖到的关键事实：

**数据模型是双口径的**，字段名直接写在 JSON 里：

| 字段 | 含义 | 来源 |
|---|---|---|
| `limit_amount` | 代销口径（天天基金/支付宝等第三方平台） | 天天基金档案页 |
| `direct_limit_amount` | 直销口径（基金公司官网/APP） | 安鑫乐量化实验室 + 微信公众号 AI 提取 |

前端硬编码的证据：
- 页面 meta 写死 `数据来源：天天基金 · {updated_at}`
- 详情弹窗写 `代销渠道：天天基金、支付宝等第三方平台`
- 持仓明细外链 `https://fundf10.eastmoney.com/ccmx_{code}.html`

**上游源有 3 种取值**（实测 `data.json` 里 57 只有 `direct_source` 的基金）：
- `anxinletech.com`（55 只）→ `https://anxinletech.com/instrument-qdii.html`
- `wechat-ai`（2 只，宝盈纳指100 A/C）→ 微信公众号文章链接
- `人工核验`（出现在 worldpage）

**关于那个 `wechat-ai` 链接**：它是作者用 AI 从微信公众号文章提取结构化额度的通道。**实测该链接已返回「链接已过期」**——微信 URL 里的 `signature` 是临时签名。此源不可依赖，本方案已弃用。

**pmtools 的架构**：作者本地跑 `python3 scanner.py --out public/qdii/data.json` 产出静态 JSON 走 nginx 托管，前端 `fetch('/qdii/data.json', {cache:'no-store'})`。不是实时查询。

**pmtools 的披露缺陷**（供参考）：前端只显示"数据来源：天天基金"，直销那一半的真实上游（anxinletech + 微信公众号）完全隐藏，`direct_source` / `direct_source_url` 只存在于 JSON 里未渲染。

---

## 三、数据源溯源：安鑫乐的上游是什么

因为我们要直连，所以必须搞清楚安鑫乐自己的数据从哪来。

### 安鑫乐自己声明的三档来源（页面原文）

> **一手来源** 按可信度分三档、逐行标注：
> `✓公告直核` ＝直接解析基金公司公告原文（PDF），并区分直销/代销渠道口径，为一手直采（按管理人分批推进，已覆盖者逐行可见并附公告全文链接）；
> `✓人工核实` ＝逐渠道实查后录入；
> `✓双源一致` ＝公告与汇总口径交叉一致。
> 未标注者为公告接口逐日抓取的汇总口径。
>
> **注意**：汇总口径只反映代销渠道，个别基金"代销暂停但直销仍可限额申购"，以公告直核与官方渠道为准。
>
> **采集方式** 程序化逐日抓取，按交易日打标签存入本地时序库，避免节假日错位

### 已验证：公告原文这条链是真的，且真含双口径

安鑫乐的公告链接指向天天基金公告页（`fund.eastmoney.com/gonggao/{code},{art_code}.html`）。该页面是 JS 渲染的，正文藏在 API 后面。**已验证可用的接口**：

```
GET https://np-cnotice-stock.eastmoney.com/api/content/ann?art_code={art_code}&client_source=web&page_index=1
```

返回 JSON，`data.notice_content` 是公告正文，`data.attach_url` 是 PDF 原件（`pdf.dfcfw.com/pdf/H2_{art_code}_1.pdf`）。

**实测 `AN202606031823188725`（大成纳斯达克100ETF联接）原文**：

> 1、2026年06月04日起，投资人通过**本公司直销渠道**（包含大成基金APP、官网、微信公众号和直销柜台等）申购本基金A类或C类份额单日单个基金账户的申购及定期定额投资累计金额应**不超过100元**人民币；
> 2、2026年06月04日起，投资人通过**各代销机构**申购本基金A类或C类份额单日单个基金账户的申购及定期定额投资累计金额应**不超过10元**人民币。

**结论：双口径（直销 100 元 / 代销 10 元，10 倍差距）是基金公司公告本身就写明的，不是安鑫乐编的。** "公告直核"标注属实。

### 完整溯源链条

```
基金公司公告原文（PDF，一手）
    ↓ 天天基金公告页转载 + 东方财富 Choice 数据
天天基金 np-cnotice-stock API（结构化 JSON 中转）
    ↓ 安鑫乐程序化逐日抓取、解析、区分渠道口径
安鑫乐 instrument-qdii.html
    ↓ 本项目抓取
我们的监控台
```

### 安鑫乐页面的数据规模（实测）

- 12 个主题表，162 只基金份额
- **104 条公告全文链接 + 58 条公告列表链接 = 162**
- 9 只渠道双列（`代销 X元 / 直销 Y元 / N×`）
- 所有行都带 `公告直核` 或 `公告列表` 标记（104 行标 `公告直核`）

主题分布：恒生科技 56、纳斯达克100 53、标普500 20、日本股市 7、道琼斯 4、德国DAX 4、亚太精选 4、印度 3、英国富时100 3、越南 3、法国CAC40 3、沙特 2。

---

## 四、已发现的问题（重要，接手请先读）

### ⚠️ 问题 1：安鑫乐的"公告直核"标签存在不实

**宝盈纳斯达克100（019736 / 019737）这一行有矛盾：**

- 安鑫乐表格：额度 **200 元**，标注 `✓公告直核`
- 它引用的公告原文 `AN202609241829831132`（2026-09-24）：**"不超过10元"**，且不分渠道
- 天天基金档案页交叉验证：`限大额（单日累计购买上限10.00元）`

**两个源都是 10 元，安鑫乐表里写 200 元还标了"公告直核"。** 这可能是旧公告的值或录入错误。

**影响**：`公告直核` 这个标签不能当作可信度背书。我们的采集器忠实反映了源站的值（200 元），问题在上游。

**建议**：加独立交叉校验（见"下一步"）。

### ⚠️ 问题 2：源站声明的 725 元无法用单一口径复现

安鑫乐页面写"今日可申购总额度：725 元（纳指100 + 标普500 方向 28 只场外基金单日限额合计）"。

实测对不上：

| 口径 | 结果 |
|---|---|
| 安鑫乐公告表的代销值合计 | 1275 元 |
| 天天基金实时口径合计（名义） | 695 元 / 26 只 |
| 天天基金实时口径合计（实际可购） | 395 元 / 24 只 |
| 源站声明 | 725 元 / 28 只 |

**根因**：两源的申购状态判定有系统性分歧——部分基金东财标"暂停申购"、公告表标"限大额"（实测 30 只场外基金属此情况），且场内份额是否计入的口径不同。

**我们的处理**：没有强行凑这个数。UI 同时展示**名义额度**与**实际可购**两个口径，并明确标注差异。

### ⚠️ 问题 3：各家公告措辞极不统一（做公告解析的难点）

要把公告原文解析做通用，会撞上措辞差异。实测三种典型：

**A. 明确区分渠道**（大成纳指100）：直销 100 元 / 代销 10 元，两句话分别写明。

**B. 只给单值**（宝盈纳指100）："本基金每一类基金份额单日每个基金账户的累计申购金额应不超过10元"，不分渠道。

**C. 按投资者类型分层**（天弘越南 008763）：
> 如**个人投资者在直销机构**单日累计申购单个基金份额金额超过 **2000 元**，本公司有权拒绝。
> 调整本基金**机构投资者在直销机构、投资者在代销机构**的大额申购业务金额限制，调整为暂停**单笔金额 1000 元以上**的申请，且单日累计不超过 **1000 元**。

天弘这只按"个人/机构 × 直销/代销"分四象限，还叠加"单笔"和"单日累计"两个维度。天弘标普500（007722）则只写"如单日累计申购单个基金份额金额超过100元，本公司有权拒绝"，不分渠道。

**建议**：不要试图写一个万能正则。应建措辞模式库，按基金公司（或公告标题关键词）匹配模板，并对不能确定的标注为"待人工核验"而非猜测。

---

## 五、已完成的交付物

### 项目结构

```
D:\deepseek\workspace\nasidake\qdii-monitor\
├── package.json          零依赖，type: module，需 Node >= 18
├── README.md             使用说明
├── .env.example          全部环境变量说明
├── src\
│   ├── config.js         配置中心（读环境变量）
│   ├── collectors.js     双口径采集器 + 解析器  ★核心
│   ├── pipeline.js       合并双口径、统计、采集流程  ★核心
│   ├── diff.js           变动检测 + 通知文案生成
│   ├── notify.js         通知通道（console/webhook×5/email）
│   ├── store.js          文件读写、时间工具
│   ├── cli.js            命令行入口
│   ├── server.js         Web UI 服务 + JSON API
│   └── scheduler.js      交易日定时调度
├── public\
│   ├── index.html        监控台页面
│   ├── styles.css        视觉系统（明暗主题、响应式）
│   └── app.js            前端逻辑
├── tools\
│   ├── show.mjs          汇总报表
│   ├── detail.mjs        明细榜单
│   ├── audit.mjs         数据质量校验
│   ├── check-ui.mjs      UI 静态一致性检查
│   ├── check-notify.mjs  通知通道验证
│   ├── e2e.mjs           端到端演练
│   ├── collect.bat       计划任务用采集脚本
│   └── start-ui.bat      一键启动 UI
└── data\
    ├── latest.json       最近快照（162 只，125 KB）
    ├── history\2026-09-24.json
    └── changes.jsonl     变动流水
```

### 采集器实现要点

**安鑫乐限额单元格的 4 种形态（实测分布）**：

| 形态 | 数量 | 样例 |
|---|---|---|
| `single` 单值 | 47 | `10元` |
| `split` 双口径 | 9 | `代销 10元 / 直销 100元 / 10×` |
| `unlimited` 正常申购 | 32 | `正常申购` |
| `none` 空 | 74 | `—`（多为暂停申购或场内份额） |

**代销口径**（`collectDistribution`）：逐只抓 `https://fund.eastmoney.com/{code}.html`，用正则解析交易状态区块：

```html
<span class="itemTit">交易状态：</span>
<span class="staticCell">限大额 (<span>单日累计购买上限10.00元</span>)</span>
<span class="staticCell">开放赎回</span>
```

并发 5、150ms 抖动、重试 3 次。**实测 162/162 成功，0 失败。**

同时解析 `purchasable` 标记：页面出现"暂不开放购买"时为 `false`（状态可能标称"限大额"但渠道实际关闭）。

**直销口径**（`collectDirect`）：一次抓 `https://anxinletech.com/instrument-qdii.html`，解析 12 个表格，得到 162 只基金。

**主题名解析的坑**：主题名写在 `<table>` 之前，前文可能同时含上一个主题的"共 N 只"，**必须取最后一个匹配**，否则标普500 的 20 只会被错标成"天弘"。

**合并逻辑**（`mergeSnapshot`）：以安鑫乐为骨架（覆盖面广、自带代码清单），把天天基金的实时代销字段合并进去。状态以代销源为准，缺失时回落公告口径。

### 统计口径设计（踩过坑后的设计）

**首次实现有个坑**：把全市场"限大额"额度直接相加，得到 9374 万——被恒生科技几只 2000 万上限的基金拉高，跟美股方向无关，毫无参考价值。

**现在的设计**：
- 额度合计只统计「**场外 + 限大额**」的基金（场内份额不占场外申购额度）
- 另算一套只针对「纳指100 + 标普500」的口径，这才是用户关心的
- 每个合计都输出两个版本：`名义`（不看渠道是否开门）和 `实际可购`（剔除 `purchasable=false`）

**命名规则**：无后缀 = 名义；`_actual` = 实际可购。

### 通知系统

支持 5 个 IM 平台（Bark / Server酱 / 钉钉 / 飞书 / 企微）+ 通用 webhook + 邮件（SMTP，用 `node:net`+`node:tls` 手写，无依赖）+ console。

**已修的真实 bug**：钉钉/飞书/企微的机器人 webhook 会返回 **HTTP 200 但响应体带错误码**（如 `errcode: 300005 "token is not exist"`）。最初只看 HTTP 状态会把"token 失效"误判成发送成功。现已解析响应体业务码，只认 `code === 0 || code === 200`。

五个平台的真实 API 均已实测响应，URL 组装正确。

### 变动检测

只追踪 4 个字段并按严重级别分类：

| 字段 | 标签 | 级别 |
|---|---|---|
| `status` | 申购状态 | high |
| `limit_amount` | 代销日累计限额 | medium |
| `direct_limit_amount` | 直销日累计限额 | medium |
| `redeem` | 赎回状态 | low |

低于 `QDII_MIN_DELTA` 的变动不通知。**无变动时静默**。

### Web UI

`node src/server.js` → `http://127.0.0.1:8787`

- 顶部并列数字：纳指100+标普500 的代销实际可投额度与直销公告口径额度
- 状态分布条形图、162 只明细表（搜索/筛选/排序）
- 点行打开详情抽屉：双口径对照、跟踪标的、跟踪误差、公告原文链接
- 变动时间线
- "立即采集"按钮

API：`/api/snapshot`、`/api/changes`、`/api/dates`、`/api/day?date=`、`/api/health`、`POST /api/collect`

### 验证状态（全部实跑通过）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 采集端到端 | `node src/cli.js collect` | 162/162 成功，0 失败 |
| 数据质量 | `node tools/audit.mjs` | 通过 |
| UI 一致性 | `node tools/check-ui.mjs` | 全部通过 |
| 通知通道 | `node tools/check-notify.mjs` | 5 平台 URL 正确，业务码识别正确 |
| 变动检测 | `node tools/e2e.mjs` | 人为造 4 类变动，精确检出 4 处 |
| 官方口径对齐 | 见"问题 2" | `tracked=162`、`buyable=120`、`channel_mismatch=9` 全部对齐 |

### 实测数据快照（2026-09-24）

```
收录 162 只份额（场内 42 / 场外 120）
开放申购 33 | 限大额 55 | 暂停申购 32 | 场内/封闭 42
代销额度有值 80 只 | 直销额度有值 56 只
渠道双列 9 只 | 公告直核标记 104 只 | 采集错误 0

恒生科技 56 只 → 开放 32 / 限额 10 / 暂停 0
纳斯达克100 53 只 → 开放 0 / 限额 22 / 暂停 19
标普500 20 只 → 开放 0 / 限额 5 / 暂停 11

美股方向场外可申购 26 只，代销合计 695 元，直销合计 3135 元
```

**渠道差异榜（最大 10 倍）**：

| 基金 | 代销 | 直销 | 倍数 |
|---|---|---|---|
| 大成标普500等权重 | 100 元 | 1000 元 | 10× |
| 大成纳斯达克100ETF联接 | 10 元 | 100 元 | 10× |
| 建信纳斯达克100 | 10 元 | 50 元 | 5× |
| 天弘越南市场 | 1000 元 | 2000 元 | 2× |

---

## 六、下一步建议（按优先级）

### 1. 公告原文交叉校验器（最高优先级）

因为发现了"公告直核"标签不实（问题 1），建议做独立校验：

```
对每只基金：
  1. 从 snapshots 取 announcement_url
  2. 提取 art_code
  3. GET np-cnotice-stock.eastmoney.com/api/content/ann?art_code={art_code}
  4. 解析 notice_content，提取直销/代销限额
  5. 与安鑫乐表里的值比对，不一致则标记出来
```

这既是质量兜底，也是将来把直销口径切换到一手解析的基础。

### 2. 公告解析器（措辞模式库）

见"问题 3"。**不要写万能正则**。建议：
- 按公告标题关键词分类（"调整大额申购"/"恢复申购"/"暂停大额申购"）
- 按基金公司匹配措辞模板
- 无法确定时标 `needs_review`，绝不猜测

已知需要覆盖的模式：
- `通过本公司直销渠道...不超过 X 元` + `通过各代销机构...不超过 Y 元`
- `每一类基金份额单日每个基金账户的累计申购金额应不超过 X 元`（不分渠道）
- `个人投资者在直销机构...超过 X 元` + `机构投资者...代销机构...≤ Y 元`（天弘四象限）

### 3. 历史数据累积与趋势

现在只有 1 天数据（`data/history/` 一个文件）。跑一段时间后可做：
- 额度松紧的时间序列图
- 识别"重开/收紧拐点"（安鑫乐自己也强调"看的是变动方向与节奏，不是单日绝对值"）
- 场内溢价监控（安鑫乐有单独的页面 `q/qdii-etf-premium-today.html`，提到"场内溢价最高 15.3%"）

### 4. 法定节假日表

`src/scheduler.js` 现在只按周一至周五判断交易日，未排除法定节假日。可接入节假日 API 或本地维护年度表。

### 5. 可选：更多数据源

- `https://fund.eastmoney.com/Fund_sgzt_bzdm.html` —— 全市场申购状态一览表（GBK 编码，需用 `TextDecoder('gbk')`），实测可访问，含"日累计/限定金额"列，可作为第三种交叉验证源
- `https://fund.eastmoney.com/data/rankhandler.aspx?op=ph&dt=kf&ft=qdii&...` —— 全量 QDII 列表，可发现新成立份额

---

## 七、踩坑记录（避免重走）

| 坑 | 表现 | 解法 |
|---|---|---|
| 微信源链接过期 | `mp.weixin.qq.com/s?...signature=...` 返回"链接已过期" | 已弃用该源 |
| pmtools 的 SPA 探测 | 任意路径都返回同一份 HTML，`robots.txt` 也是 | 必须从 JS bundle 里挖路由 |
| 主题名错位 | 标普500 的 20 只被标成"天弘" | 表前文本取**最后一个**"共 N 只"匹配 |
| 限额单值形态漏解析 | 47 只单值行解析为 null | 加 `single` 形态分支 |
| "正常申购"未识别 | 32 只被当无数据 | 加 `unlimited` 分支 |
| 额度合计虚高 | 全市场相加得 9374 万，被恒生科技 2000 万拉高 | 限定方向统计，分名义/实际两口径 |
| 标称限大额但实际关闭 | 页面标"限大额"却写"暂不开放购买" | 加 `purchasable` 标记 |
| IM webhook 假成功 | HTTP 200 但 body 是错误码 | 解析业务码，不只信 HTTP 状态 |
| 端口占用 | 8787 被上一次验证的进程占着 | 检查后复用或换端口 |
| 天天基金页面编码 | 部分页面是 GBK | 用 `Buffer` + `TextDecoder('gbk')` |

---

## 八、快速上手

```bash
cd D:\deepseek\workspace\nasidake\qdii-monitor

node --version           # 需 >= 18，无第三方依赖

node src/cli.js collect  # 采集（首次建立基线）
node src/cli.js report   # 看摘要
node src/server.js       # 启动 UI → http://127.0.0.1:8787

# 报表
node tools/show.mjs      # 汇总
node tools/detail.mjs    # 明细榜单

# 验证
node tools/audit.mjs
node tools/check-ui.mjs
node tools/check-notify.mjs
node tools/e2e.mjs
```

通知配置（环境变量，见 `.env.example`）：

```bash
export QDII_NOTIFY=console,webhook
export QDII_WEBHOOK_KIND=bark        # bark|serverchan|dingtalk|feishu|wecom|generic
export QDII_WEBHOOK_TOKEN=your_token
export QDII_INDEXES=nasdaq100,sp500  # 只监控美股方向
node src/cli.js collect
```

---

## 九、调查过程产生的辅助文件

以下位于 `D:\deepseek\workspace\nasidake\_recon\`，是调查阶段的原始证据，**可保留供查证**：

| 文件 | 内容 |
|---|---|
| `index-DKUSia-s.js` | pmtools 的前端 bundle（808 KB），双口径字段证据来源 |
| `qdii_data.json` | pmtools 的 `/qdii/data.json`（378 KB），89 只基金 |
| `qdii_worldpage-data.json` | pmtools 的跨市场数据，84 只 |
| `qdii_simulation-data.json` | pmtools 的定投回测数据，320 个月度点 |
| `anxin.html` | 安鑫乐页面完整 HTML（104 KB），溯源证据 |
| `anxin_parsed.json` | 安鑫乐 162 只的解析结果 |
| `ann2.mjs` / `ann3.mjs` / `ann4.mjs` | 公告原文 API 探测脚本 |
| `verify019736.mjs` | 宝盈数据矛盾的验证脚本 |
| `wx.html` | 微信源"链接已过期"的证据 |

---

## 十、注意事项

- **天天基金对高频访问敏感**。默认并发 5、150ms 抖动、重试 3 次。**不要调高并发**，否则会被限流。
- 数据源自公开渠道，仅供个人参考，**不构成投资建议**。
- 本项目的 `README.md` 是给最终用户的使用说明；**本文档是给开发者的交接文档**，两者定位不同。
